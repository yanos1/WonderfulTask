# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Secrets
.env

# Logs / temp
*.log

# Raw bulk downloads (large; regenerated by etl.py). The derived data/airports.json
# IS committed so the app runs without re-running the ETL.
data/raw/
